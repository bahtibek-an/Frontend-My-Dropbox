 <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="/">Dropbox</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto">
        <% if user_signed_in? %>
      <li class="nav-item">
         <%= link_to 'Edit Profile', edit_user_registration_path, class: 'nav-link blue-button' %>
      </li>
        <li class="nav-item">
         <%= button_to 'Sign Out', destroy_user_session_path, method: :delete, class: 'nav-link blue-button' %>
       </li>
       <% end %>
      </ul>
    </div>
  </nav>
  <style>
/* Add this style to your custom.css or equivalent stylesheet */
.blue-button {
  background-color: #0061ff; /* Blue background color */
  color: #fff; /* White text color */
  font-weight: bold; /* Make the text bold */
  border: none;
  padding: 10px 20px; /* Adjust the padding as needed */
  border-radius: 5px; /* Rounded corners */
  text-align: center;
  text-decoration: none;
  display: inline-block;
  font-size: 16px;
  margin: 4px 2px;
  cursor: pointer;
}

.blue-button:hover {
  background-color: #0053df; /* Darker blue on hover */
}

</style>